import styled from "styled-components";

const LoginModal = () => {
  return <ModalContainer>hi</ModalContainer>;
};

export default LoginModal;

const ModalContainer = styled.div`
  width: 430px;
  height: 790px;
  background-color: #ffffff;
  border-radius: 10px;
`;
