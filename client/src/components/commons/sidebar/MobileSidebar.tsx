// MobileSidebar.tsx
import styled from "styled-components";
import BaseSidebar from "./BaseSidebar";

const MobileSidebar = styled(BaseSidebar).attrs({variant: "mobile"})``;

export default MobileSidebar;
